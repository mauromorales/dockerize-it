## dockerfiles-opensuse-osc

Using osc to build packages and interact with the [open build service](http://openbuildservice.org/)

### running the container

run the container with --privileged option
