dockerfiles-opensuse-python
===========================

OpenSUSE Dockerfile for python - interpreted, interactive, object-oriented, extensible programming language  


To build:

```
# docker build -t <username>/python .
```

To run: 

```
# docker run -it <username>/ruby ipython
```
